import { PhotoCamera, ImportContacts, Info, Home, Note, Receipt, Settings } from "@material-ui/icons";

const Icons = {
    "Home": <Home />,
    "Note": <Note />,
    "Receipt": <Receipt />,
    "Settings": <Settings />,
    "PhotoCamera": <PhotoCamera />,
    "Info": <Info />,
    "Default": <ImportContacts />,
}

export default Icons;